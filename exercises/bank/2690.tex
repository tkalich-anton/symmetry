\begin{ex}
	\begin{condition}
		Даны окружности радиусов \( r  \) и \( R (R > r) \). Расстояние	между их центрами равно \( a (a > R+r) \). Найдите отрезки общих внешних и общих внутренних касательных, заключенные между точками касания.
	\end{condition}
	\answer{\( \sqrt{a^2-(R+r)^2} \)}
\end{ex}