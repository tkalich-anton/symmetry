%СТИЛИ

\usepackage{cmap}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english, russian]{babel}
\usepackage[dvipsnames,table,xcdraw]{xcolor}
\usepackage{
	amsmath,
	amssymb,
	scrextend,
	enumitem,
	pscyr,
	multicol,
	cmap,
	titling,
	indentfirst,
	cancel,
	wrapfig,
	gensymb,
	tikz,
	graphicx,
	fancyhdr,
	mathrsfs,
	graphbox,
	indentfirst,
	import,
	tabularx,
	colortbl,
	tablestyles,
	titling,
	xparse,
	xifthen,
	verbatim,
	currfile,
	xhfill
}

%БЛОК КОМАНДЫ

\newcommand{\leveli}{\textcolor{dark}{\footnotesize $\blacksquare\square\square$}\hspace{0.3em}}
\newcommand{\levelii}{\textcolor{dark}{\footnotesize $\blacksquare\blacksquare\square$}\hspace{0.3em}}
\newcommand{\leveliii}{\textcolor{dark}{\footnotesize $\blacksquare\blacksquare\blacksquare$}\hspace{0.3em}}
\newcommand{\selectanswer}{\noindent\textit{В ответе укажите номер правильного варианта.}}

%Нумированный список со столбцами

\newlist{enumcols}{enumerate}{5}
\setlist[enumcols]{label=\arabic*)}

\SetEnumitemKey{columns}{before=\begin{multicols}{#1},
		after=\end{multicols}}

%Команды, связанные с математикой

\def\le{\leqslant}
\def\ge{\geqslant}
\DeclareUnicodeCharacter{202F}{\,}
\newcommand{\bb}[1]{\left(#1\right)}

% Переменные
\newboolean{show_number}
\newboolean{show_answer}
\newboolean{show_rule}

\newcommand{\numbertype}{real_number}
\newcommand{\bankpath}{\@empty}

% Команды, которые задают значения переменных
\newcommand{\shownumber}[1]{\setboolean{show_number}{#1}}
\newcommand{\showanswer}[1]{\setboolean{show_answer}{#1}}
\newcommand{\showrule}[1]{\setboolean{show_rule}{#1}}
\newcommand{\setBankpath}[1]{\renewcommand\bankpath{#1}}

%\newcommand{\num}{\@empty}

%\newcommand{\setnumber}[1]{\renewcommand\num{#1}}

\NewDocumentEnvironment{ex}{O{\boolean{show_number}}O{\boolean{show_rule}}}
{%
	\ifthenelse {#1}
	{%
		\noindent
		\ifthenelse {\equal{\numbertype}{real_number}}%
		{\textbf{\textit{\textnumero\currfilebase}}\\[1em]}
		{\textbf{\textit{Без номера}}\\[1em]}
	}%
	{}
}
{
	\ifthenelse {#2}
	{\noindent\xhrulefill{silver}{.2mm}\\[0.2pt]\par}
	{\noindent\xhrulefill{silver}{0mm}\\[0.2pt]\par}
}

\newcommand{\answer}[1]{
	\ifthenelse {\boolean{show_answer}}
	{\textcolor{silver}{\fbox{#1}}}
	{}
}

\newcommand{\ranswer}[1]{{
		
		\raggedleft
		\textcolor{silver}{\fbox{#1}}
		
}}

\newcommand{\graphcenter}[1]{
\begin{center}
\includegraphics{#1}
\end{center}
}

%\newcommand{\funcexer}[2]{
%	\begin{minipage}[t]{0.7\textwidth}
%		#1
%	\end{minipage}
%	\begin{minipage}[c]{0.25\textwidth}
%		\includegraphics[align=t, width=\textwidth]{#2}
%	\end{minipage}
%}

%СЕТТЕРЫ

%Параметры страницы
\usepackage[left=15mm,right=15mm,
top=2cm,bottom=2cm]{geometry}
\pagestyle{fancy}

%Путь к картинкам
\graphicspath{{pic/}}
\DeclareGraphicsExtensions{.pdf,.png,.jpg}

\DeclareUnicodeCharacter{202F}{\,}

%Русские символы в списке
\AddEnumerateCounter{\asbuk}{\russian@alph}{щ}

\setlength{\parindent}{2ex}

%Название школы в шапке справа
\rhead{Школа <<Симметрия>>}

%Значение переменных по умолчанию
\shownumber{false}
\showanswer{false}
\showrule{false}